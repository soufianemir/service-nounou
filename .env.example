# Database (Supabase Postgres)
# Runtime (serverless): use the Supabase pooler URL.
DATABASE_URL="postgresql://USER:PASSWORD@POOLER_HOST:6543/postgres?schema=public&sslmode=require&pgbouncer=true"
# For db push/migrations, use the direct URL temporarily:
# postgresql://USER:PASSWORD@db.<ref>.supabase.co:5432/postgres?schema=public&sslmode=require

# Auth (JWT session cookie)
SESSION_JWT_SECRET="change-me-to-a-long-random-string"
SESSION_COOKIE_NAME="cn_session"
SESSION_COOKIE_SECURE="false"  # set true behind HTTPS in prod

# App base URL
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Storage (optional local, disabled by default)
# FILE_STORAGE_DIR="./storage"

# Legal (France)
NEXT_PUBLIC_LEGAL_PUBLISHER_NAME="CarnetNounou"
NEXT_PUBLIC_LEGAL_PUBLISHER_STATUS=""
NEXT_PUBLIC_LEGAL_PUBLISHER_ADDRESS=""
NEXT_PUBLIC_LEGAL_CONTACT_EMAIL="contact@exemple.fr"
NEXT_PUBLIC_LEGAL_HOST_NAME=""
NEXT_PUBLIC_LEGAL_HOST_ADDRESS=""
